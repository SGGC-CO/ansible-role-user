---
galaxy_info:
  role_name: ansible-role-user
  author: alikarimii
  description: add user and set password
  company: Software Genesis Group
  license: "license (BSD, MIT)"
  min_ansible_version: 2.1
  github_branch: main
  github_repo: https://github.com/SGGC-CO/ansible-role-user.git
  platforms:
    - name: Ubuntu
      versions:
        - all
  galaxy_tags: [orchestration, ssh, createuser]
dependencies:
  []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
